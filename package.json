{
  "name": "prettier-tasks",
  "license": "MIT",
  "devDependencies": {
    "eslint-config-prettier": "2.6.0",
    "eslint-plugin-prettier": "2.3.1",
    "eslint": "4.8.0",
    "husky": "0.14.3",
    "lint-staged": "4.2.3",
    "prettier": "1.7.4"
  },
  "lint-staged": {
    "*.js": ["npm run lint:script", "git add"],
    "*.json": ["npm run lint:json", "git add"],
    "*.scss": ["npm run lint:style", "git add"]
  },
  "scripts": {
    "lint": "npm run lint:script && npm run lint:style && npm run lint:json",
    "lint:script": "eslint --fix -c .eslintrc.yaml --ignore-path .gitignore **/*.js ",
    "lint:style": "prettier --single-quote --print-width=80 --write '**/*.scss'",
    "lint:json": "prettier --single-quote --print-width=120 --write '**/*.json'",
    "precommit": "lint-staged"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lipis/prettier-tasks"
  },
  "private": true
}
